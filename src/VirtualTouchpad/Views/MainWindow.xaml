<Window x:Class="VirtualTouchpad.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Virtual Touchpad"
        Width="300" Height="220"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="True"
        ResizeMode="NoResize">

    <Border CornerRadius="8" BorderBrush="#555" BorderThickness="1">
        <Border.Background>
            <SolidColorBrush Color="#2D2D2D" />
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />   <!-- Title bar -->
                <RowDefinition Height="*" />    <!-- Touchpad area -->
                <RowDefinition Height="40" />   <!-- Click buttons -->
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="#1E1E1E" CornerRadius="8,8,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Menu icon -->
                    <TextBlock Grid.Column="0" Text="&#x2261;" FontSize="18"
                               Foreground="#AAA" VerticalAlignment="Center"
                               Margin="10,0,5,0" />

                    <!-- Title -->
                    <TextBlock Grid.Column="1" Text="Virtual Touchpad" FontSize="11"
                               Foreground="#888" VerticalAlignment="Center" />

                    <!-- Window controls -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal"
                                VerticalAlignment="Center" Margin="0,0,4,0">
                        <!-- Minimize button -->
                        <Button Content="&#x2500;" Width="28" Height="24"
                                Style="{StaticResource TitleBarButton}"
                                Click="MinimizeButton_Click"
                                ToolTip="Minimize" />
                        <!-- Close button -->
                        <Button Content="&#x00D7;" Width="28" Height="24"
                                Style="{StaticResource CloseButton}"
                                Click="CloseButton_Click"
                                ToolTip="Close" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Touchpad Area -->
            <Border x:Name="TouchpadArea" Grid.Row="1"
                    Background="#383838" Margin="1,0,1,0"
                    Cursor="None">
                <TextBlock Text="Touchpad" FontSize="12"
                           Foreground="#555" HorizontalAlignment="Center"
                           VerticalAlignment="Center" IsHitTestVisible="False" />
            </Border>

            <!-- Click Buttons -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="1" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Left Click"
                        Style="{StaticResource ClickButton}"
                        Click="LeftClickButton_Click" />

                <Rectangle Grid.Column="1" Fill="#555" />

                <Button Grid.Column="2" Content="Right Click"
                        Style="{StaticResource ClickButton}"
                        Click="RightClickButton_Click" />
            </Grid>
        </Grid>
    </Border>

    <Window.Resources>
        <!-- Title bar button style -->
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#AAA" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="4,0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#444" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close button style -->
        <Style x:Key="CloseButton" TargetType="Button"
               BasedOn="{StaticResource TitleBarButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C42B1C" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Click button style -->
        <Style x:Key="ClickButton" TargetType="Button">
            <Setter Property="Background" Value="#333" />
            <Setter Property="Foreground" Value="#BBB" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="0,0,8,8" Padding="0,8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#444" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#555" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
</Window>
